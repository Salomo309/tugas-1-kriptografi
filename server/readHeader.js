/**
 * Membaca konten file sebagai header dalam bentuk string.
 *
 * @param {string} fileContent Konten file dalam bentuk string.
 * @returns {string} Header dalam bentuk string.
 */
function readFileHeader(fileContent) {
  // Assuming file content contains the header directly
  return fileContent;
}

module.exports = {
  readFileHeader,
};
